[package]
name = "write-to-account"
version = "0.1.0"
edition = "2021"

[dependencies]
solana-program = { path = "../../../../sdk/program", version = "=2.2.0" }

[lib]
crate-type = ["cdylib", "rlib"]

[workspace]
